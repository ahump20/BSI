{
  "name": "sandlot-sluggers",
  "private": true,
  "version": "1.0.0",
  "description": "Sandlot Sluggers - Original backyard baseball minigame for BlazeSportsIntel.com",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "validate-glb": "node scripts/validate-glb.mjs",
    "deploy": "npm run build && npx wrangler deploy --config wrangler.jsonc"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "terser": "^5.27.0",
    "typescript": "^5.6.3",
    "vite": "^7.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
