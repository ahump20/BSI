name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
      - staging
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy to Cloudflare Pages
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9
      
      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      
      - name: Build project
        run: pnpm build
      
      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy public --project-name=${{ secrets.CLOUDFLARE_PAGES_PROJECT }} --branch=${{ github.ref_name }} --commit-dirty=true

      - name: Wait for deployment propagation
        if: github.ref == 'refs/heads/main'
        run: sleep 30

      - name: Verify API Health
        if: github.ref == 'refs/heads/main'
        run: |
          echo "🏥 Running API health checks..."

          HEALTH_CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://blazesportsintel.com/api/health" || echo "000")
          if [ "$HEALTH_CODE" = "200" ]; then
            echo "✅ API Health: Healthy"
          else
            echo "⚠️ API Health check returned: $HEALTH_CODE"
          fi

          SECRETS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://blazesportsintel.com/api/admin/secrets-status" || echo "000")
          if [ "$SECRETS_CODE" = "200" ]; then
            echo "✅ Secrets Status: All required secrets configured"
          elif [ "$SECRETS_CODE" = "503" ]; then
            echo "⚠️ Secrets Status: Some secrets missing"
          fi

      - name: Verify API Providers
        if: github.ref == 'refs/heads/main'
        run: |
          echo "🔌 Testing external API providers..."

          RESPONSE=$(curl -s "https://blazesportsintel.com/api/admin/api-tests/all" || echo "{}")
          STATUS=$(echo "$RESPONSE" | jq -r '.summary.overallStatus // "unknown"')
          PASSED=$(echo "$RESPONSE" | jq -r '.summary.passed // 0')
          FAILED=$(echo "$RESPONSE" | jq -r '.summary.failed // 0')

          echo "  • Passed: $PASSED | Failed: $FAILED | Status: $STATUS"

          if [ "$STATUS" = "healthy" ]; then
            echo "✅ All API providers healthy"
          else
            echo "⚠️ Some providers degraded - check /api/admin/api-tests/all"
          fi

      - name: Verify Documentation
        if: github.ref == 'refs/heads/main'
        run: |
          DOC_CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://blazesportsintel.com/docs/" || echo "000")
          if [ "$DOC_CODE" = "200" ]; then
            echo "✅ API Documentation available at /docs/"
          else
            echo "⚠️ Documentation returned: $DOC_CODE"
          fi
