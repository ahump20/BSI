import BlazeSportsCommandCenter from '../../components/BlazeSportsCommandCenter';

export const metadata = {
  title: 'Enterprise Dashboard | Blaze Sports Intel',
  description: 'Real-time sports analytics dashboard with MLB, NFL, NBA data',
};

export default function DashboardPage() {
  return <BlazeSportsCommandCenter />;
}
