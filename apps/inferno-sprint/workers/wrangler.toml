# BSI: Inferno Sprint - Leaderboard Worker Configuration
name = "bsi-inferno-sprint-leaderboard"
main = "leaderboard.ts"
compatibility_date = "2024-01-15"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "production"
GAME_VERSION = "1.0.0"

# KV namespace for leaderboard storage
[[kv_namespaces]]
binding = "LEADERBOARD_KV"
id = "0898dd7e9a564bb7ac57e19c7a928c8b"

# Secrets (set via wrangler secret put):
# SCORE_SECRET - Secret for anti-cheat hash validation
