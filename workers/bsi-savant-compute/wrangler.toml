# BSI Savant Compute — Cron Worker
# Recomputes advanced metrics (wOBA, FIP, wRC+, etc.) from player_season_stats
# every 6 hours. Manual trigger via GET /compute.
#
# Deploy: wrangler deploy --config workers/bsi-savant-compute/wrangler.toml

name = "bsi-savant-compute"
main = "index.ts"
compatibility_date = "2025-12-01"
compatibility_flags = ["nodejs_compat"]

# Enable the *.workers.dev preview subdomain so the synthetic monitor's
# self-heal path can POST to https://bsi-savant-compute.ahump20.workers.dev/run
# when sabermetric tables go stale. Without this flag, the URL NXDOMAINs.
workers_dev = true

[observability]
enabled = true

# Run 30 min after bsi-cbb-analytics (which runs on the hour every 6h)
# Ensures fresh sabermetric data in D1 before NIL scoring reads it
[triggers]
crons = ["30 */6 * * *"]

[[d1_databases]]
binding = "DB"
database_name = "bsi-prod-db"
database_id = "6921617f-5351-4df3-aeab-07425e72ec6b"

[[kv_namespaces]]
binding = "KV"
id = "e6cb68e815cd45babeee0e10460dbfee"

[[tail_consumers]]
service = "bsi-error-tracker"

[dev]
port = 8795
