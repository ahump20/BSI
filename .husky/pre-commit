#\!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run linting
echo "→ Running ESLint..."
npm run lint
if [ $? -ne 0 ]; then
  echo "❌ ESLint failed. Please fix linting errors before committing."
  exit 1
fi

# Run formatting check
echo "→ Checking code formatting..."
npm run format
if [ $? -ne 0 ]; then
  echo "❌ Formatting check failed. Run 'npm run format:fix' to auto-format."
  exit 1
fi

# Run type checking (temporarily disabled - pre-existing errors in codebase)
# echo "→ Running TypeScript type check..."
# npm run typecheck
# if [ $? -ne 0 ]; then
#   echo "❌ Type checking failed. Please fix type errors before committing."
#   exit 1
# fi

echo "✅ All pre-commit checks passed\!"

